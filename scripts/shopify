#!/usr/bin/env zsh
set -euo pipefail

# Ensure Shopify CLI runs on a supported Node version in non-interactive shells.
export NVM_DIR="$HOME/.nvm"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  source "$NVM_DIR/nvm.sh"
  nvm use 20 >/dev/null
fi

shopify "$@"
